<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main_navigation"
    app:startDestination="@id/navigation_menu">
    <fragment
        android:id="@+id/navigation_menu"
        android:name="com.thodsaphon.fastfood.ui.menu.MenuFragment"
        android:label="@string/menu"
        tools:layout="@layout/fragment_menu" />
    <fragment
        android:id="@+id/navigation_cart"
        android:name="com.thodsaphon.fastfood.ui.cart.CartFragment"
        android:label="@string/cart"
        tools:layout="@layout/fragment_cart" />
    <fragment
        android:id="@+id/navigation_bill"
        android:name="com.thodsaphon.fastfood.ui.bill.BillFragment"
        android:label="@string/bill"
        tools:layout="@layout/fragment_bill" />
</navigation>